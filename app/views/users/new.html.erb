<fieldset>
  <legend>
    <h1><%= t(:register) %></h1>
  </legend>

  <%= form_for @user do |f| %>
    <%= f.error_messages %>
    <%= render :partial => 'form', :object => f %>
    <%= f.submit t(:register) %>
  <% end %>
</fieldset>
